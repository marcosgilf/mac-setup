# nvim

Personal LazyVim configuration with the Tokyo Night theme.

Place this directory at Neovim's configuration path or select it with
`NVIM_APPNAME`.

The plugin lockfile is tracked for repeatable plugin versions.
